# Nonauthoritative clean-room estimates

None of the alternatives below has been promoted to the manuscript. Its claims, constants and ranges remain V3. Promotion requires a separate targeted audit and a new freeze.

| Quantity | Authoritative V3 bound | First frozen reproduction (CR0_A_sol) | Second frozen reproduction (CR0_B_fresh_agent) |
|---|---|---|---|
| Main-ratio CV squared | >= 289/(132710400 pi epsilon) - 1, 0 < epsilon <= 1/4 | >= 729/(268435456 pi epsilon) - 1; stronger coefficient | >= 25/(2359296 pi epsilon) - 1, report Eq. (8.4); stronger coefficient |
| Reciprocal CV squared | >= 1/(1679616 pi eta^2) - 1, eta > 0, 0 < eta^2 <= 1/16 | >= 1/(2985984 pi eta^2) - 1; weaker coefficient | Same V3 coefficient, report Eq. (9.4) |
| Fixed-purity ratio | (5/(18 epsilon))(1-epsilon/10) <= r <= 5/(18 epsilon), 0 < epsilon < 5/18 | Coarser lower bound 175/(648 epsilon), same upper bound | Reconstructs the same fixed-purity no-go and exact fractional-linear ratio |

Sources: the two frozen CR0_REPORT.md files in the complete bundle, the first report's Family C, Family D and Family E sections (the last bound is tagged Q), the second report Sections 8-10, and comparison/CRB_COMPARISON.json. The comparison file identifies CR0_A_sol by archive hash 8db63b... despite its CRB filename. It must not be misattributed to the second frozen archive 433591e1....

All alternatives are reproduction evidence about supplied families, not evidence of independent discovery or human validation. No proposal is made here to optimize any bound. The finite-dimensional scope of the clean-room factor-two result does not certify V3's conditional infinite-dimensional extension, which the manuscript omits.
